export interface LevelConfig {
  worldWidth: number;
  worldHeight: number;
  bgTileKey: string;
}

export const level1Config: LevelConfig = {
  worldWidth: 3840,
  worldHeight: 540,
  bgTileKey: 'bg-tile',
};
